import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#ec407a',
    },
    secondary: {
      main: '#ab47bc',
    },
    background: {
      default: '#fff',
    },
  },
});

export default theme;
